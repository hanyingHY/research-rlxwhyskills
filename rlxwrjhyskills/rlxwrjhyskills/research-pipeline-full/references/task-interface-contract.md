# Task Interface Contract

## Objective

Give every worker folder one stable interface file so downstream windows and users can see what the worker owns, consumes, and produces.

## Required file

Every worker folder should contain `task_interface.md`.

## Required sections

1. role
2. owned scope
3. inputs
4. outputs
5. done when
6. do not touch
